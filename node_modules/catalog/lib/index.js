// Generated by CoffeeScript 1.6.3
(function() {
  var Catalog, Connection, Q, Table, mysql;

  Q = require('kew');

  mysql = require('mysql');

  Connection = require('mysql/lib/Connection');

  Table = require('./lib/table');

  Catalog = (function() {
    function Catalog(infoOrHandler, tables) {
      var _this = this;
      if (tables == null) {
        tables = [];
      }
      if (infoOrHandler instanceof Connection) {
        this.connection = infoOrHandler;
      } else {
        this.connection = mysql.createConnection(info);
      }
      tables.forEach(function(table) {
        return Object.defineProperty(_this, table, {
          enumerable: false,
          get: _this.getTable.bind(_this, tables)
        });
      });
    }

    return Catalog;

  })();

}).call(this);
